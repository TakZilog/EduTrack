node_modules
vendor
api/config.php

# Claude Code / ruflo MCP tooling artifacts, not part of the app
ruvector.db
ruvector.db.lock
agentdb.rvf
agentdb.rvf.lock
.claude-flow/
.agents/

# Map snapshots written by the admin panel before each change
storage/
assets/nodes/nodes-edges.json.lock
.claude/.caveman-mode
tools/cloudflared.exe
sql/edutrack_backup.sql

# Environment files (secrets). None exist today; this keeps a future one out of git.
.env
.env.*
!.env.example

# Per-machine editor and tooling state. Kept on disk, never shared.
.idea/
.swarm/
.impeccable/questions/

# Built by tools/import-photos.php --build; goes live with --publish
assets/nodes-new/

# Per-machine Claude settings. Commands approved in a session are saved here,
# and one can carry a password.
.claude/settings.local.json
